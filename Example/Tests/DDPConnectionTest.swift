//
//  DDPConnectionTest.swift
//  DDP
//
//  Created by Peter Siegesmund on 9/10/15.
//  Copyright © 2015 Peter Siegesmund. All rights reserved.
//

import Foundation
import Quick
import Nimble
/*
class DDPConnectionTest:QuickSpec {
    override func spec() {
        describe ("DDP Connection") {
            
            it ("can connect to a DDP server"){
                var connected = false
                let client = DDPClient(url: url) {result, error in
                    if (error == nil) {
                        connected = true
                    }
                }
                expect(client.connected).toEventually(beTrue())
                expect(connected).toEventually(beTrue())
            }
            
        }
    }
}
*/